# 🚀 دليل وتقرير نشر مشروع StorShoes

هذا الملف يحتوي على الخطوات العملية التي يجب عليك تنفيذها الآن لنشر الـ API الخاص بك على Railway وربطه بالواجهة الأمامية (Storefront) الموجودة على Vercel.

يمكنك استخدام هذا الملف كـ "قائمة تحقق" (Checklist) لتتبع تقدمك.

---

## 🟢 المرحلة الأولى: إعداد قاعدة البيانات (PostgreSQL)

لأن مشروعك يحتاج إلى حفظ البيانات (المنتجات، الطلبات، إلخ)، تحتاج إلى قاعدة بيانات على الإنترنت.

- [ ] **1. إنشاء قاعدة بيانات على Railway:**
  - سجل الدخول إلى [Railway.app](https://railway.app/).
  - اضغط على **"New Project"**.
  - اختر **"Provision PostgreSQL"**.
  - انتظر ثوانٍ حتى يتم إنشاؤها.
- [ ] **2. نسخ رابط قاعدة البيانات:**
  - اضغط على قاعدة البيانات التي أنشأتها للتو.
  - اذهب إلى تبويب **"Variables"** أو **"Connect"**.
  - انسخ قيمة المتغير `DATABASE_URL` (سيبدأ بـ `postgresql://...`).
  - **ألصق الرابط هنا للاحتفاظ به:**
    > `DATABASE_URL`: `________________________________________________`

---

## 🔵 المرحلة الثانية: نشر الـ API (NestJS) على Railway

الآن سنقوم بنشر الكود الخلفي (Backend) الذي يعالج البيانات.

- [ ] **1. ربط مستودع GitHub:**
  - في نفس مشروع Railway (الذي يحتوي على قاعدة البيانات)، اضغط على زر **"New"** (علامة `+` في الأعلى).
  - اختر **"GitHub Repo"** وحدد مستودع مشروعك `stor-shoes`.
- [ ] **2. إعداد مسار الـ API:**
  - بمجرد إضافة المستودع، اضغط عليه في واجهة Railway.
  - اذهب إلى تبويب **"Settings"**.
  - انزل إلى قسم **"Build"** أو **"Environment"**.
  - غيّر **"Root Directory"** إلى: `/apps/api`
- [ ] **3. إعداد أوامر البناء والتشغيل:**
  - في نفس المكان (Settings -> Build):
  - اجعل **"Build Command"**: `npm run build && npx prisma generate && npx prisma migrate deploy`
  - اجعل **"Start Command"**: `npm run start:prod`
- [ ] **4. إضافة متغيرات البيئة للـ API:**
  - اذهب إلى تبويب **"Variables"** في الـ API الخاص بك على Railway وأضف الآتي:
    - `DATABASE_URL` ⬅️ *(نفس الرابط الذي نسخته في المرحلة الأولى)*
    - `JWT_SECRET` ⬅️ `my-super-secret-key-prod-2026` *(أو أي كلمة سر معقدة من اختيارك)*
    - `JWT_EXPIRATION` ⬅️ `7d`
    - `ALLOWED_ORIGINS` ⬅️ `https://storshoes.vercel.app` *(غيّره إذا كان رابط Vercel الخاص بك مختلفاً)*
- [ ] **5. توليد رابط عام للـ API:**
  - اذهب إلى تبويب **"Settings"** وانزل إلى قسم **"Networking"**.
  - اضغط على **"Generate Domain"** للحصول على رابط للـ API الخاص بك.
  - **ألصق الرابط هنا للاحتفاظ به:**
    > `API URL`: `________________________________________________`

---

## 🟠 المرحلة الثالثة: ربط الـ Storefront (Vercel) بالـ API الجديد

الآن الـ API الخاص بك يعمل على الإنترنت. يجب أن نخبر الواجهة الأمامية بمكانه.

- [ ] **1. الدخول إلى إعدادات Vercel:**
  - اذهب إلى لوحة تحكم [Vercel](https://vercel.com/) وافتح مشروع الـ Storefront الخاص بك.
- [ ] **2. تحديث متغيرات البيئة:**
  - اذهب إلى **Settings** ➡️ **Environment Variables**.
  - ابحث عن المتغير `NEXT_PUBLIC_API_URL`.
  - قم بتحديث قيمته ليكون الرابط الجديد الذي حصلت عليه من Railway، مع إضافة `/api` في نهايته.
  - **القيمة الجديدة يجب أن تكون هكذا تقريباً:** `https://your-app-name.up.railway.app/api`
- [ ] **3. إعادة نشر الواجهة (Redeploy):**
  - في Vercel، اذهب إلى تبويب **"Deployments"**.
  - اضغط على النقاط الثلاث بجوار أحدث عملية نشر ناجحة واختر **"Redeploy"**.
  - هذا ضروري لكي يقرأ الموقع الرابط الجديد للـ API.

---

## ✅ المرحلة الرابعة: الاختبار النهائي

بعد الانتهاء من جميع الخطوات أعلاه:

- [ ] افتح رابط متجرك على Vercel.
- [ ] هل تظهر المنتجات والتصنيفات في الصفحة الرئيسية بدلاً من "لا توجد منتجات متاحة حالياً"؟
- [ ] هل يمكنك فتح صفحة الـ Swagger الخاصة بالـ API لتجربته؟ (عبر الرابط `https://[رابط-railway]/api/docs`)

**إذا قمت بوضع علامة (✓) على جميع هذه الخطوات، فمبروك! متجرك الآن يعمل بالكامل على بيئة الإنتاج.**
